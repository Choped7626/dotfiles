# Add blur to waybar
layerrule = blur, size:2, passes:3, vibrancy:0.1696, waybar
layerrule = blurpopups, waybar
layerrule = ignorealpha 0.7, waybar

layerrule = blur size 2 passes 3 vibrancy 0.1696, fuzzel
layerrule = blurpopups, fuzzel
layerrule = ignorealpha 0.7, fuzzel

# Color Picker for unity
windowrule = move cursor, class:^(Unity)$, title:^(Color)$
windowrule = move cursor, class:^(Unity)$, title:^(HDR Color)$

# Fix some dragging issues with XWayland
windowrule = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0################
### MONITORS ###
################

monitor=,preferred,auto,auto
monitor=eDP-1,preferred,auto,1.8
monitor=HDMI-A-1,preferred,0x0,1

###################
### MY PROGRAMS ###
###################

$terminal = kitty
$fileManager = nautilus
$menu = fuzzel

#################
### AUTOSTART ###
#################

exec-once = waybar & swaync & hypridle & hyprpaper & kanshi & nm-applet
exec-once = Pictures/back/.scripts/random-wallpaper-selector.sh &

#############################
### ENVIRONMENT VARIABLES ###
#############################

env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24

###################
### PERMISSIONS ###
###################

#####################
### LOOK AND FEEL ###
#####################

general {
    gaps_in = 5
    gaps_out = 8

    border_size = 1

    col.active_border = rgb(d8cab8)
    col.inactive_border = rgb(AC82E9)

    resize_on_border = true

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = dwindle
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 6

    active_opacity = 1.0
    inactive_opacity = 0.9

    # Window Shadow
    shadow:enabled = true
    shadow:range = 16
    shadow:render_power = 5
    shadow:color = rgba(0,0,0,0.2)

    # Transparent Window Blur
    blur:enabled = true

    blur:size = 2
    blur:passes = 3
    blur:vibrancy = 0.1696
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = true

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    animation = global, 1, 10, default
    animation = border, 1, 5.39, easeOutQuint
    animation = windows, 1, 4.79, easeOutQuint
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    animation = windowsOut, 1, 1.49, linear, popin 87%
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 3.03, quick
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear
    animation = workspaces, 1, 1.94, almostLinear, fade
    animation = workspacesIn, 1, 1.21, almostLinear, fade
    animation = workspacesOut, 1, 1.94, almostLinear, fade
}

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more

# https://wiki.hyprland.org/Configuring/Variables/#misc
#misc {
#    force_default_wallpaper = 0 # Set to 0 or 1 to disable the anime mascot wallpapers
#    disable_hyprland_logo = true # If true disables the random hyprland logo / anime girl background. :(
#}


#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = es
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = true
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#gestures
gestures {
    gesture = 3, horizontal, workspace
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}


###################
### KEYBINDINGS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = ALT CTRL # Sets "Windows" key as main modifier

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, T, exec, $terminal
bind = $mainMod, C, killactive,
bind = SUPER, L, exec, hyprlock
bind = $mainMod, L, exit,
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, V, togglefloating,
bind = $mainMod, space, exec, $menu
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, J, togglesplit, # dwindle
bind = $mainMod, F, exec, firefox
bind = $mainMod, B, exec, $terminal -e bluetui

bind =, PRINT, exec, hyprshot -m window -o ~/Pictures/Screenshots
bind = shift, PRINT, exec, hyprshot -m region -o ~/Pictures/Screenshots

$midMod = SUPER

# Move focus with mainMod + arrow keys
bind = $midMod, left, movefocus, l
bind = $midMod, right, movefocus, r
bind = $midMod, up, movefocus, u
bind = $midMod, down, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $midMod, 1, workspace, 1
bind = $midMod, 2, workspace, 2
bind = $midMod, 3, workspace, 3
bind = $midMod, 4, workspace, 4
bind = $midMod, 5, workspace, 5
bind = $midMod, 6, workspace, 6
bind = $midMod, 7, workspace, 7
bind = $midMod, 8, workspace, 8
bind = $midMod, 9, workspace, 9
bind = $midMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $midMod SHIFT, 1, movetoworkspace, 1
bind = $midMod SHIFT, 2, movetoworkspace, 2
bind = $midMod SHIFT, 3, movetoworkspace, 3
bind = $midMod SHIFT, 4, movetoworkspace, 4
bind = $midMod SHIFT, 5, movetoworkspace, 5
bind = $midMod SHIFT, 6, movetoworkspace, 6
bind = $midMod SHIFT, 7, movetoworkspace, 7
bind = $midMod SHIFT, 8, movetoworkspace, 8
bind = $midMod SHIFT, 9, movetoworkspace, 9
bind = $midMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $midMod, S, togglespecialworkspace, magic
bind = $midMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $midMod, mouse_down, workspace, e+1
bind = $midMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $midMod, mouse:272, movewindow
bindm = $midMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl -e4 -n2 set 5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 5%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

##############################
### WINDOWS AND WORKSPACES ###
##############################
# Color Picker for unity
windowrule = move cursor, class:^(Unity)$, title:^(Color)$
windowrule = move cursor, class:^(Unity)$, title:^(HDR Color)$

# Fix some dragging issues with XWayland
windowrule = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0

